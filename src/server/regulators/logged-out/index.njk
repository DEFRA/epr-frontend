{% extends 'layouts/page.njk' %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <h1 class="govuk-heading-l">{{ localise('regulators:loggedOut:heading') }}</h1>
      <p class="govuk-body">{{ localise('regulators:loggedOut:body', { serviceName: localise('regulators:serviceName') }) }}</p>
      {{ govukButton({
        href: signInAgainHref,
        text: localise('regulators:loggedOut:signInAgain')
      }) }}
    </div>
  </div>
{% endblock %}
