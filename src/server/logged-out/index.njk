{% extends 'layouts/page.njk' %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <h1 class="govuk-heading-l">{{ localise('logged-out:heading') }}</h1>
      <p class="govuk-body">{{ localise('logged-out:body', { serviceName: localise('common:serviceName') }) }}</p>
      {{ govukButton({
        href: localiseUrl('/login'),
        text: localise('logged-out:signInAgain')
      }) }}
    </div>
  </div>
{% endblock %}
