{% extends 'layouts/page.njk' %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <h1 class="govuk-heading-l">{{ localise('sign-out:heading') }}</h1>
      <p class="govuk-body">{{ localise('sign-out:body') }}</p>
      {{ govukButton({
        href: localiseUrl('/login'),
        text: localise('sign-out:signInAgain')
      }) }}
    </div>
  </div>
{% endblock %}
