{% extends 'layouts/page.njk' %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <div class="govuk-body" data-testid="app-page-body">
        {% if isDefraIdEnabled %}
          <h1 class="govuk-heading-l">Link Gaskells Waste Services to its registration</h1>
          <p class="govuk-body">As you're the first person from your organisation to access this service, you need to link the organisation to a registration we've previously received.</p>
          <p class="govuk-body">You'll only need to confirm this once, on behalf of the organisation you represent.</p>
          <h2 class="govuk-heading-m">Select the registration to link to Gaskells Waste Services</h2>

          {# TODO insert a radio group to select the org - can be a list of one #}

          {# TODO add values for organisation name #}
          {# TODO add translations #}

          {# <h1 class="govuk-heading-l">{{ localise('account:linking:heading') }}</h1> #}
          {# <p class="govuk-body">{{ localise('account:linking:subheading') }}</p> #}
        {% endif %}
      </div>
    </div>
  </div>

{% endblock %}
